<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group11.cmpt276_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.ui.InspectionDetailActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|orientation|keyboardHidden|screenSize"/>
        <activity android:name=".view.ui.RestuarantDetailActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|orientation|keyboardHidden|screenSize"/>
        <activity android:name=".view.ui.RestaurantListActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|orientation|keyboardHidden|screenSize"/>
        <activity android:name=".view.ui.MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>